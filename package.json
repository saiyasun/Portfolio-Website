{
  "scripts": {
    "prebuild": "npm run generate:posts",
    "build": "npm run generate:blog-pages && npm run generate:sitemap && tsc -p tsconfig.json",
    "generate:blog-pages": "node scripts/generate-blog-pages.mjs",
    "generate:posts": "node scripts/generate-posts-json.mjs",
    "generate:sitemap": "node scripts/generate-sitemap.mjs",
    "watch": "tsc -w -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
