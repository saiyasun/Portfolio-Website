// Used by the statically generated article pages. The article itself is HTML,
// but it still needs the shared navigation, translator, and language behavior.
import "../styles/global.css";
import "../styles/pages/blog-post.css";
import "../components/navbar/navbar";
import "../components/translator/translator";
import "../lib/i18n";
